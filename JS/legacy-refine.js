(()=>{'use strict';const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];document.documentElement.classList.add('legacy-page');const body=document.body;if(!body)return;const main=$('main,.horizontal-scroll,.member,.faq-container,#blog-entries,.thanks')||body;if(!main.id)main.id='main-content';if(!$('.skip-link')){const skip=document.createElement('a');skip.className='skip-link';skip.href='#'+main.id;skip.textContent='Skip to content';body.prepend(skip)}const nav=$('.nav-links'),toggle=$('.hamburger-icon');if(toggle&&nav){toggle.setAttribute('role','button');toggle.setAttribute('tabindex','0');toggle.setAttribute('aria-controls',nav.id||(nav.id='primary-nav'));toggle.setAttribute('aria-expanded','false');const setMenu=open=>{nav.classList.toggle('show',open);toggle.setAttribute('aria-expanded',String(open))};toggle.onclick=()=>setMenu(!nav.classList.contains('show'));toggle.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle.click()}if(e.key==='Escape')setMenu(false)};$$('a',nav).forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('click',e=>{if(!nav.contains(e.target)&&!toggle.contains(e.target))setMenu(false)})}const page=(location.pathname.split('/').pop()||'index.html').toLowerCase();$$('a[href]').forEach(a=>{const href=(a.getAttribute('href')||'').split('#')[0].toLowerCase();if(href&&href===page)a.setAttribute('aria-current','page')});const scroller=$('.horizontal-scroll');if(scroller){scroller.setAttribute('tabindex','0');scroller.setAttribute('aria-label',scroller.getAttribute('aria-label')||'Horizontal page sections');const sections=$$('.scroll-section',scroller);const go=delta=>{const current=Math.round(scroller.scrollLeft/Math.max(scroller.clientWidth,1));const next=Math.max(0,Math.min(sections.length-1,current+delta));sections[next]?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'nearest',inline:'start'})};$$('.round').forEach(control=>{const direction=control.closest('.left-con')||control.querySelector('.previous')?-1:1;control.setAttribute('role','button');control.setAttribute('tabindex','0');control.setAttribute('aria-label',direction<0?'Previous section':'Next section');control.onclick=e=>{e.preventDefault();go(direction)};control.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();control.click()}}});scroller.addEventListener('keydown',e=>{if(e.key==='ArrowRight')go(1);if(e.key==='ArrowLeft')go(-1)});scroller.addEventListener('wheel',e=>{if(Math.abs(e.deltaY)>Math.abs(e.deltaX)&&innerWidth>700){e.preventDefault();scroller.scrollBy({left:e.deltaY,behavior:'auto'})}},{passive:false});const progress=document.createElement('div');progress.className='legacy-progress';progress.setAttribute('aria-hidden','true');progress.innerHTML='<span></span>';body.append(progress);const meter=$('span',progress);scroller.addEventListener('scroll',()=>{const max=scroller.scrollWidth-scroller.clientWidth;meter.style.width=(max?scroller.scrollLeft/max*100:0)+'%'},{passive:true})}$$('.faq-item').forEach((item,i)=>{const q=$('.faq-question',item),answer=$('.faq-answer',item);if(!q||!answer)return;const id='faq-answer-'+(i+1);q.setAttribute('role','button');q.setAttribute('tabindex','0');q.setAttribute('aria-expanded','false');q.setAttribute('aria-controls',id);answer.id=id;answer.setAttribute('role','region');answer.style.maxHeight='0';const activate=()=>{const open=item.classList.toggle('open');q.setAttribute('aria-expanded',String(open));answer.style.maxHeight=open?answer.scrollHeight+'px':'0'};q.onclick=activate;q.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();activate()}}});$$('.flip-card').forEach(card=>{card.setAttribute('tabindex','0');card.setAttribute('aria-label','Interactive project card; focus or hover to reveal details')});const words={"about2-heading":['About Us'],"contact2-heading":['Contact Us'],"portfolio-heading":['Portfolio'],"intro-heading":['Hi There!']};Object.entries(words).forEach(([id,list])=>{const el=document.getElementById(id);if(!el||el.textContent.trim())return;const word=list[0];if(matchMedia('(prefers-reduced-motion: reduce)').matches){el.textContent=word;return}let n=0;const cursor=document.createElement('span');cursor.setAttribute('aria-hidden','true');cursor.textContent='_';el.setAttribute('aria-label',word);const tick=()=>{el.textContent=word.slice(0,n++);el.append(cursor);if(n<=word.length)setTimeout(tick,75)};tick()});$$('img').forEach((img,i)=>{if(!img.alt)img.alt=i===0?'Anchor Web Design visual':'Studio project visual';if(i>1)img.loading='lazy';img.decoding='async'});requestAnimationFrame(()=>document.documentElement.classList.add('legacy-page-ready'))})();
